
<div class="tabs">
	{{#each tab in content}}
		<span class="{{if tab.active 'active'}}" {{action 'changeTab' tab bubbles=false}}>{{tab.tabName}}</span>
	{{/each}}
</div>

<div class="tab-content">
	{{yield}}
</div>
