import parse from 'dummy/utils/parse';
import { module, test } from 'qunit';

module('Unit | Utility | parse');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parse();
  assert.ok(result);
});
