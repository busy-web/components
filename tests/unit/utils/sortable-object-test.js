import sortableObject from 'dummy/utils/sortable-object';
import { module, test } from 'qunit';

module('Unit | Utility | sortable object');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortableObject;
  assert.ok(result);
});
