
<div class="box">
	{{checkbox-input id=inputId name=name group=group checked=checked _value=value disabled=disabled onChange="inputChanged"}}
	<label for="{{inputId}}"></label>
</div>

{{#if title}}
	<span>{{{title}}}</span>
{{/if}}
