import { throwAlert } from 'dummy/helpers/throw-alert';
import { module, test } from 'qunit';

module('Unit | Helper | throw alert');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = throwAlert([42]);
  assert.ok(result);
});
