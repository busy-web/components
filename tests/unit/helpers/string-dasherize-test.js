import { stringDasherize } from 'dummy/helpers/string-dasherize';
import { module, test } from 'qunit';

module('Unit | Helper | string dasherize');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringDasherize([42]);
  assert.ok(result);
});
