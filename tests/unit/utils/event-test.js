import { bind, unbind } from 'dummy/utils/event';
import { module, test } from 'qunit';

module('Unit | Utility | event');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.ok(bind);
  assert.ok(unbind);
});
