
<div class="tabs">
	{{#each tab in view.content}}
		<span class={{if tab.active 'active'}} {{action 'changeTab' tab bubbles=false target='view'}}>{{tab.tabName}}</span>
	{{/each}}
</div>

<div class="tab-content">
	{{yield}}
</div>
