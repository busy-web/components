<div class="tabs">
	{{#each tab in view.tabNames}}
	<span {{bind-attr class="tab.active:active"}} {{action 'changeTab' tab bubbles=false target='view'}}>{{tab.tabName}}</span>
	{{/each}}
</div>
<div class="tab-content">
	{{yield}}
</div>
